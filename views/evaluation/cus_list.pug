extends ../layout/emp_layout.pug

block content
  h1.main-title 평가 가능 프로젝트 목록 (고객)
  .container
    .card.mb-3
      .card-header
        .card-body
          .table-responsive
            table#dataTable.table.table-bordered(width='100%', cellspacing='0')
              thead
                tr 
                  th ID
                  th 프로젝트명
                  th 시작일
                  th 종료일
                  th 평가하기
              tbody
                each p in projects
                  tr 
                    td= p.p_id
                    td= p.p_name
                    td= p.p_start_date
                    td= p.p_end_date
                    td
                      a.btn.btn-outline-info(href=`/evaluation/${p.p_id}`) 평가하기


